import menus from './menus.json'
import palette from './palette.json'

const {
  links,
  sidebar: sidebarList
  // header: shortcutList
} = menus

export {
  palette,
  links,
  sidebarList
  // shortcutList
}
